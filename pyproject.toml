[project]
name = "eclipse-id"
version = "0.1.0"
description = "API para rastreamento de imagem"
authors = ["Seu Nome <seu@email.com>"]
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn"
]
